{
  "scripts": {
    "test": "jest",
    "proxy": "nodemon --env-file=.env proxy/app.js",
    "start": "nodemon --env-file=.env api/app.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
